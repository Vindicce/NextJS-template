import { Wrapped, Text } from '~/components/Basics';

import { I{{ pascalCase name }}Layout } from '~/components/{{ pascalCase name }}/data'

export const {{pascalCase name}} = ({ data }: I{{pascalCase name}}Layout ) => (
  <Wrapped flex={1} testID={`{{pascalCase name}}`}>
    <Text color="ORANGE">{`{{pascalCase name}}`}</Text>
  </Wrapped>
)